import { observe, setTitle, zoom } from "./dom";
import { eventEmitter } from "./event_emitter";
import { randomInt } from "./math";
import { clsx, removeSpaces } from "./string";
import { debounce, delay, getPeriod, throttle } from "./time";
import { log, typeOf } from "./vanilla";

export {
  clsx,
  debounce,
  delay,
  eventEmitter,
  getPeriod,
  log,
  observe,
  randomInt,
  removeSpaces,
  setTitle,
  throttle,
  typeOf,
  zoom
};